// Initialize AOS (Animate On Scroll)
AOS.init({
  duration: 800,  // Animation duration in ms
  easing: 'ease-in-out',
  once: true,     // Animation only once
  mirror: false,  // Disable repeating animation
});
